@model OnlineStore.Models.LoginModel

@{
    Layout = null;
}
@if (ViewBag.IsAuthenticated)
{
    <h2>Hi , @ViewBag.Username</h2>
    <p><a href="@Url.Action("Logout","Login")" class="btn btn-danger btn-block">登出</a> </p>
}
else
{
    <ul class="nav justify-content-end topside">
        <li class="nav-item">
            <a class="nav-link" href="#" data-toggle="modal" data-target="#signinmodel">LOGIN</a>
        </li>
        <li class="nav-item">
            <a class="nav-link" href="#" data-toggle="modal" data-target="#registermodel">REGISTER</a>
        </li>
        <li class="nav-item">
            <a class="nav-link" href="#">
                <i class="fas fa-user"></i>
            </a>
        </li>
        <li class="nav-item">
            <a class="nav-link" href="#">
                <i class="fas fa-shopping-basket"></i>
            </a>
        </li>
    </ul>
}